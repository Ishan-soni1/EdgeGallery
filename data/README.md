# Test data

Do not commit private gallery photos to this directory. Create a small local
dataset from images you own or openly licensed images, then add:

- an exact byte-for-byte copy;
- a resized or recompressed copy;
- an unrelated image;
- a deliberately dark image;
- a deliberately bright image.

The `.gitignore` keeps the actual files local while retaining this guide.
